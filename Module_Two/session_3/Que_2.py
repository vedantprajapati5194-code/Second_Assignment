# Que_2:-  Create a list of restaurant ratings (e.g., [4.2, 3.8, 4.9, 2.5, 4.0]) and use the sorted() function to display the ratings in descending order.


ratings = [4.2, 3.8, 4.9, 2.5, 4.0]

sorted_ratings = sorted(ratings, reverse=True)

print("Original ratings:", ratings)
print("Ratings in descending order:", sorted_ratings)